<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="me.weicools.material.component.alert.dialog">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/alert_dialog_ic_launcher"
        android:label="@string/alert_dialog_name"
        android:roundIcon="@mipmap/alert_dialog_ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".AlertDialogActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>